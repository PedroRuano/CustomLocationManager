import XCTest
@testable import CostumLocationManager

final class CostumLocationManagerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CostumLocationManager().text, "Hello, World!")
    }
}
